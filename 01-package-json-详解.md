# 📦 package.json 完全指南

## 🎯 什么是 package.json？

`package.json` 是 Node.js 项目的**配置文件**，就像是项目的"说明书"。

**类比理解：**
- 如果你的项目是一台电脑
- `package.json` 就是"配置清单"
- 告诉别人这台电脑需要安装哪些软件（依赖）
- 以及如何启动这台电脑（脚本命令）

---

## 📋 package.json 的核心字段

### 1️⃣ 基本信息

```json
{
  "name": "webnavigator-homepage",    // 项目名称（必填）
  "version": "0.0.0",                 // 版本号（必填）
  "private": true,                    // 是否私有（不发布到 npm）
  "type": "module"                    // 模块类型（ES Module）
}
```

**解释：**
- `name`: 项目的唯一标识符
- `version`: 遵循语义化版本（Semantic Versioning）
  - 格式：`主版本号.次版本号.修订号`
  - 例如：`1.2.3` 表示主版本1，次版本2，修订3
- `private: true`: 防止意外发布到 npm 公共仓库
- `type: "module"`: 使用现代 ES6 模块语法（`import/export`）

---

### 2️⃣ scripts - 脚本命令（重要！）

```json
{
  "scripts": {
    "dev": "vite",                    // 开发模式
    "build": "vite build",            // 构建生产版本
    "preview": "vite preview"         // 预览构建结果
  }
}
```

**如何使用：**

| 命令 | 实际执行 | 作用 | 使用场景 |
|------|----------|------|----------|
| `npm run dev` | `vite` | 启动开发服务器 | 开发时使用 |
| `npm run build` | `vite build` | 打包生产代码 | 部署前使用 |
| `npm run preview` | `vite preview` | 预览打包结果 | 测试打包效果 |

**注意：** `dev` 可以简写为 `npm run dev`，但 `start` 可以简写为 `npm start`（不需要 run）

---

### 3️⃣ dependencies - 生产依赖

```json
{
  "dependencies": {
    "echarts": "^6.0.0",              // 图表库
    "vue": "^3.5.25",                 // Vue 框架
    "vue-echarts": "^8.0.1"           // Vue + ECharts 集成
  }
}
```

**特点：**
- 这些包会被打包到最终的生产代码中
- 用户访问网站时需要这些代码才能运行
- 例如：Vue 框架、ECharts 图表库

**版本号说明：**
- `^6.0.0` - 表示兼容 6.x.x 的最新版本
  - ✅ 可以升级到 6.1.0, 6.2.5
  - ❌ 不会升级到 7.0.0
- `~6.0.0` - 表示兼容 6.0.x 的最新版本
  - ✅ 可以升级到 6.0.1, 6.0.9
  - ❌ 不会升级到 6.1.0
- `6.0.0` - 精确版本，不会自动升级

---

### 4️⃣ devDependencies - 开发依赖

```json
{
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.0",    // Tailwind CSS 插件
    "@vitejs/plugin-vue": "^6.0.2",   // Vite 的 Vue 插件
    "tailwindcss": "^4.2.0",          // Tailwind CSS
    "vite": "^7.3.1"                  // Vite 构建工具
  }
}
```

**特点：**
- 这些包只在开发时使用
- 不会被打包到最终的生产代码中
- 例如：构建工具（Vite）、CSS 框架（Tailwind）

**区别总结：**

| 类型 | 打包到生产代码 | 示例 |
|------|----------------|------|
| `dependencies` | ✅ 是 | Vue、ECharts |
| `devDependencies` | ❌ 否 | Vite、Tailwind |

---

## 🔧 npm 命令详解

### 1️⃣ npm install（安装依赖）

```bash
# 安装 package.json 中的所有依赖
npm install
```

**执行过程：**
1. 读取 `package.json` 中的 `dependencies` 和 `devDependencies`
2. 下载所有包到 `node_modules/` 文件夹
3. 生成/更新 `package-lock.json`（锁定精确版本）

**结果：**
```
你的项目/
├── node_modules/        ← 所有依赖都在这里
│   ├── vue/
│   ├── vite/
│   ├── echarts/
│   └── ...
├── package.json
└── package-lock.json    ← 锁定版本号
```

---

### 2️⃣ npm install <package>（安装新包）

```bash
# 安装到 dependencies
npm install vue

# 安装到 devDependencies
npm install -D vite

# 简写
npm i vue
npm i -D vite
```

**区别：**
- 不加 `-D`：安装到 `dependencies`（生产依赖）
- 加 `-D`：安装到 `devDependencies`（开发依赖）

---

### 3️⃣ npm run <script>（运行脚本）

```bash
# 运行 scripts 中定义的命令
npm run dev       # 执行 "dev": "vite"
npm run build     # 执行 "build": "vite build"
npm run preview   # 执行 "preview": "vite preview"
```

**注意：**
- `npm run` 不是"运行整个项目"
- 而是"运行 scripts 中定义的某个命令"

---

## 🎓 实际操作示例

### 场景1：第一次克隆项目

```bash
# 1. 克隆项目
git clone <仓库地址>

# 2. 进入项目
cd webnavigator-homepage

# 3. 安装依赖（必须！）
npm install

# 4. 启动开发服务器
npm run dev
```

---

### 场景2：开发新功能

```bash
# 1. 启动开发服务器
npm run dev

# 2. 打开浏览器访问 http://localhost:5173
# 3. 修改代码，浏览器自动刷新
# 4. 完成开发后，按 Ctrl+C 停止服务器
```

---

### 场景3：部署到生产环境

```bash
# 1. 构建生产版本
npm run build

# 2. 查看生成的文件
ls dist/

# 3. 预览构建结果（可选）
npm run preview

# 4. 将 dist/ 文件夹部署到服务器
```

---

## 🧪 动手练习

### 练习1：查看依赖

```bash
# 查看项目依赖树
npm list
## 🔍 npm list 的查找机制详解

### npm 如何查找 package.json？

**核心规则：npm 会向上递归查找 `package.json`**

```
/Users/jim/                           ← 这里有 package.json（全局安装的包）
└── 0-Phd/
    └── Back-to-future/
        └── Web2Tool/
            └── webNavigator-homepage/
                ├── webNavigator_homepage/   ← 这里有 package.json（项目依赖）
                └── webNaivgator_homepage_edu/  ← 这里没有 package.json
```

### 实际测试结果

#### 1️⃣ 在有 package.json 的目录运行

```bash
cd webNavigator_homepage/
npm list
```

**输出：**
```
webnavigator-homepage@0.0.0 /Users/jim/.../webNavigator_homepage
├── @tailwindcss/vite@4.2.0
├── @vitejs/plugin-vue@6.0.4
├── echarts@6.0.0
├── tailwindcss@4.2.0
├── vite@7.3.1
├── vue-echarts@8.0.1
└── vue@3.5.28
```

✅ **显示当前项目的依赖**

---

#### 2️⃣ 在没有 package.json 的目录运行

```bash
cd webNaivgator_homepage_edu/
npm list
```

**输出：**
```
/Users/jim
├── @ai-sdk/gateway@1.0.0 extraneous
├── @anthropic-ai/sdk@0.33.1 extraneous
├── openai@4.104.0 extraneous
└── ... 其他全局包
```

⚠️ **向上查找，找到了 `/Users/jim/` 目录下的 package.json**

---

## 📊 总结对比表

| 命令 | 作用 | 是否持续运行 | 生成文件 |
|------|------|--------------|----------|
| `npm install` | 安装依赖 | ❌ 否 | `node_modules/` |
| `npm run dev` | 开发服务器 | ✅ 是 | 无（内存中） |
| `npm run build` | 构建生产版本 | ❌ 否 | `dist/` |
| `npm run preview` | 预览构建结果 | ✅ 是 | 无（读取 dist/） |

---

## ❓ 常见问题

### Q1: 为什么要区分 dependencies 和 devDependencies？

**A:** 为了优化生产环境的包大小。

- 生产依赖（dependencies）会被打包到最终代码
- 开发依赖（devDependencies）只在开发时使用，不会增加用户下载的文件大小

### Q2: package-lock.json 是什么？

**A:** 锁定精确版本号的文件。

- `package.json` 中 `^6.0.0` 可能安装 6.1.0 或 6.2.0
- `package-lock.json` 锁定精确版本，确保团队成员安装的版本一致

---

## 🎯 下一步学习

现在你已经理解了 `package.json`，接下来应该学习：

1. ✅ **Vite 配置** - 理解 `vite.config.js`
2. ✅ **Vue 3 基础** - 理解 `.vue` 文件结构
3. ✅ **项目结构** - 理解 `src/` 目录的组织方式

准备好了吗？我们继续下一步！
