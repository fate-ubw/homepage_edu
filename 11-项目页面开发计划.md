# 🎨 WebNavigator 项目页面开发计划

## 📋 项目需求确认

### ✅ 已确认的需求

1. **风格定位**
   - ✅ 现代风格（渐变、动画）
   - ✅ 简洁、专业的学术风格
   - ✅ 排版漂亮大气

2. **功能需求**
   - ✅ 添加动画效果（不过分花哨）
   - ✅ 添加深色模式
   - ⏸️ 响应式设计（手机端）- 后期添加

3. **内容结构**
   - ✅ Title + Authors + Abstract（第一阶段）
   - ⏸️ 其他部分（后续添加）

---

## 🎯 第一阶段：Hero Section（标题区域）

### **包含内容：**

1. **Title（标题）**
   ```
   WebNavigator: Global Web Navigation
   via Interaction Graph Retrieval
   ```

2. **Authors（作者）**
   ```
   Xuanwang Zhang¹'²'³'*
   Yuteng Han¹'²'*
   Jinnan Qi¹'²
   Mulong Xie³
   Zhen Wu¹'²'†
   Xinyu Dai¹'²
   
   ¹ National Key Laboratory for Novel Software Technology, Nanjing University
   ² School of Artificial Intelligence, Nanjing University
   ³ Fellou AI
   
   * Equal contribution
   † Corresponding author
   ```

3. **Links（链接按钮）**
   - 📄 Paper（论文）
   - 📚 arXiv
   - 💻 Code（GitHub）
   - 🎮 Visualization（可视化）

4. **Teaser Figure（主图）**
   - 展示项目的核心概念图

---

## 🎨 设计风格参考

### **顶级学术项目页面风格**

参考项目：
- **CLIP** (OpenAI): 简洁、渐变背景、大标题
- **Segment Anything** (Meta): 现代、动画、视频展示
- **GPT-4** (OpenAI): 专业、渐变、卡片式布局
- **LLaMA** (Meta): 学术、简洁、重点突出

### **我们的设计方向**

```
顶部导航栏（固定）
  ↓
Hero Section（渐变背景）
  ├─ arXiv Badge（橙色标签）
  ├─ 标题（大字体、渐变色）
  ├─ 作者列表（可点击）
  ├─ 机构信息（小字体）
  ├─ 链接按钮（彩色、悬停效果）
  └─ 主图（阴影、圆角）
  ↓
Abstract Section（白色背景）
  ├─ 摘要文字
  └─ 关键数据卡片（渐变背景）
  ↓
其他 Sections（后续添加）
```

---

## 🛠️ 技术实现方案

### **第1步：安装 Tailwind CSS**

```bash
npm install -D tailwindcss @tailwindcss/vite
```

**原因：**
- Tailwind CSS 是现代化的 CSS 框架
- 快速实现渐变、动画、响应式
- 原项目也在使用

---

### **第2步：创建组件结构**

```
src/
├── App.vue                    ← 主应用
├── components/
│   ├── Navigation.vue         ← 导航栏
│   ├── HeroSection.vue        ← 标题区域（第一阶段重点）
│   ├── AbstractSection.vue    ← 摘要区域（第一阶段重点）
│   └── ThemeToggle.vue        ← 深色模式切换按钮
└── style.css                  ← 全局样式
```

---

### **第3步：实现 HeroSection 组件**

#### **数据结构：**

```javascript
const authors = [
  { 
    name: 'Xuanwang Zhang', 
    affiliations: [1, 2, 3],
    isEqual: true,  // Equal contribution
    url: '#' 
  },
  { 
    name: 'Yuteng Han', 
    affiliations: [1, 2],
    isEqual: true,
    url: '#' 
  },
  // ... 其他作者
]

const affiliations = [
  { id: 1, name: 'National Key Laboratory for Novel Software Technology, Nanjing University' },
  { id: 2, name: 'School of Artificial Intelligence, Nanjing University' },
  { id: 3, name: 'Fellou AI' }
]

const links = [
  { name: 'Paper', icon: '📄', url: '#', color: 'red' },
  { name: 'arXiv', icon: '📚', url: 'https://arxiv.org/...', color: 'orange' },
  { name: 'Code', icon: '💻', url: 'https://github.com/...', color: 'gray' },
  { name: 'Visualization', icon: '🎮', url: '#visualization', color: 'blue' }
]
```

---

#### **样式特点：**

1. **渐变背景**
   ```css
   background: linear-gradient(to bottom, #EFF6FF, #FFFFFF)
   ```

2. **标题动画**
   ```css
   /* 淡入动画 */
   @keyframes fadeIn {
     from { opacity: 0; transform: translateY(20px); }
     to { opacity: 1; transform: translateY(0); }
   }
   ```

3. **按钮悬停效果**
   ```css
   /* 悬停时向上移动 + 阴影 */
   transition: transform 0.2s, box-shadow 0.2s;
   hover: transform: translateY(-2px); box-shadow: 0 10px 20px rgba(0,0,0,0.1);
   ```

4. **深色模式**
   ```css
   .dark {
     background: linear-gradient(to bottom, #1F2937, #111827);
     color: #F9FAFB;
   }
   ```

---

### **第4步：实现 AbstractSection 组件**

#### **内容结构：**

```
Abstract 标题
  ↓
摘要文字（大段落）
  ↓
关键数据卡片（3列）
  ├─ 72.9% Success Rate
  ├─ 2× Better than Enterprise
  └─ 6 Action Space Size
```

#### **样式特点：**

1. **卡片渐变**
   ```css
   background: linear-gradient(135deg, #EFF6FF 0%, #E0E7FF 100%)
   ```

2. **数字动画**
   ```javascript
   // 数字从 0 增长到目标值
   const animateValue = (start, end, duration) => {
     // 实现数字滚动效果
   }
   ```

---

### **第5步：实现深色模式**

#### **方案：**

```javascript
// 使用 Vue 的响应式数据
const isDark = ref(false)

// 切换主题
function toggleTheme() {
  isDark.value = !isDark.value
  document.documentElement.classList.toggle('dark')
}
```

#### **样式：**

```css
/* 亮色模式 */
:root {
  --bg-primary: #FFFFFF;
  --text-primary: #111827;
  --accent: #3B82F6;
}

/* 深色模式 */
.dark {
  --bg-primary: #111827;
  --text-primary: #F9FAFB;
  --accent: #60A5FA;
}
```

---

## 📊 实现优先级

### **第一阶段（本次实现）**

1. ✅ 安装 Tailwind CSS
2. ✅ 创建 HeroSection 组件
   - 标题
   - 作者列表
   - 机构信息
   - 链接按钮
3. ✅ 创建 AbstractSection 组件
   - 摘要文字
   - 关键数据卡片
4. ✅ 实现深色模式切换
5. ✅ 添加基础动画效果

### **第二阶段（后续）**

- ⏸️ Visualization Section
- ⏸️ Method Section
- ⏸️ Results Section
- ⏸️ Citation Section
- ⏸️ Footer

---

## 🎨 颜色方案

### **亮色模式**

```
主色：蓝色系
- Primary: #3B82F6 (blue-500)
- Secondary: #1E40AF (blue-800)
- Accent: #60A5FA (blue-400)

背景：
- 主背景: #FFFFFF
- 次背景: #F9FAFB (gray-50)
- 卡片背景: 渐变 (blue-50 → indigo-50)

文字：
- 主文字: #111827 (gray-900)
- 次文字: #6B7280 (gray-500)
```

### **深色模式**

```
主色：浅蓝色系
- Primary: #60A5FA (blue-400)
- Secondary: #93C5FD (blue-300)
- Accent: #3B82F6 (blue-500)

背景：
- 主背景: #111827 (gray-900)
- 次背景: #1F2937 (gray-800)
- 卡片背景: 渐变 (gray-800 → gray-700)

文字：
- 主文字: #F9FAFB (gray-50)
- 次文字: #9CA3AF (gray-400)
```

---

## 🎬 动画效果

### **1. 页面加载动画**

```css
/* 淡入 + 向上移动 */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-title {
  animation: fadeInUp 0.8s ease-out;
}

.hero-authors {
  animation: fadeInUp 0.8s ease-out 0.2s;
  animation-fill-mode: backwards;
}
```

---

### **2. 悬停动画**

```css
/* 按钮悬停 */
.link-button {
  transition: all 0.3s ease;
}

.link-button:hover {
  transform: translateY(-3px);
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
}

/* 作者名悬停 */
.author-name:hover {
  color: #3B82F6;
  transform: scale(1.05);
}
```

---

### **3. 数字滚动动画**

```javascript
// 关键数据卡片的数字从 0 增长
function animateNumber(element, target, duration) {
  let start = 0
  const increment = target / (duration / 16)
  
  const timer = setInterval(() => {
    start += increment
    if (start >= target) {
      element.textContent = target
      clearInterval(timer)
    } else {
      element.textContent = Math.floor(start)
    }
  }, 16)
}
```

---

## 📝 需要确认的问题

### **1. 图片资源**

- ❓ 是否有主图（Teaser Figure）？
- ❓ 图片文件名是什么？
- ❓ 是否需要我先用占位图？

### **2. 链接地址**

- ❓ arXiv 链接是什么？
- ❓ GitHub 仓库链接是什么？
- ❓ 是否有论文 PDF 链接？

### **3. 作者信息**

- ❓ 是否需要作者个人主页链接？
- ❓ 是否需要显示邮箱？
- ❓ 通讯作者是 Zhen Wu，对吗？

### **4. Abstract 内容**

- ❓ 摘要文字是否已经准备好？
- ❓ 关键数据（72.9%, 2×, 6）是否正确？
- ❓ 是否需要修改这些数据？

---

## 🚀 开始实现

### **现在可以开始的步骤：**

1. **安装 Tailwind CSS**
   ```bash
   cd webNaivgator_homepage_edu
   npm install -D tailwindcss @tailwindcss/vite
   ```

2. **配置 Tailwind**
   - 修改 `vite.config.js`
   - 修改 `src/style.css`

3. **创建 HeroSection 组件**
   - 实现标题
   - 实现作者列表
   - 实现链接按钮

4. **创建 AbstractSection 组件**
   - 实现摘要文字
   - 实现关键数据卡片

5. **实现深色模式**
   - 添加切换按钮
   - 实现主题切换逻辑

---

## 💬 你的决定

请回答以下问题，我们就可以开始实现了：

1. **是否现在开始安装 Tailwind CSS？**
2. **是否有主图（fig2-v10.svg）？如果没有，是否先用占位图？**
3. **arXiv 和 GitHub 链接是什么？**
4. **摘要文字是否需要我从 LaTeX 中提取？**
5. **是否还有其他问题或调整？**

回答完这些问题，我们立即开始实现！🎉
