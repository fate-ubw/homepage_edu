# 🏗️ 从零构建 GitHub Pages 项目 - 完整指南

## 🎯 构建顺序总览

```
第1步：使用脚手架创建项目骨架
  ↓
第2步：安装依赖
  ↓
第3步：启动开发服务器，确认能运行
  ↓
第4步：理解项目结构
  ↓
第5步：开始编写代码
  ↓
第6步：配置部署
```

---

## 第1步：使用 Vite 脚手架创建项目（5分钟）

### ❌ 错误做法（不要这样做！）

```bash
# 不要手动创建这些文件！
touch package.json
touch index.html
mkdir src
```

**为什么不对？**
- `package.json` 格式复杂，手写容易出错
- 缺少必要的配置文件
- 缺少构建工具配置

---

### ✅ 正确做法：使用官方脚手架

**什么是脚手架（Scaffolding）？**
- 就像"项目模板生成器"
- 自动创建所有必要的文件和配置
- 确保项目结构符合最佳实践

**实际操作：**

```bash
# 1. 进入你的学习目录
cd /Users/jim/0-Phd/Back-to-future/Web2Tool/webNavigator-homepage/webNaivgator_homepage_edu

# 2. 使用 Vite 创建 Vue 项目
npm create vite@latest . -- --template vue

# 解释：
# - npm create vite@latest  ← 使用 Vite 脚手架
# - .                       ← 在当前目录创建（不创建新文件夹）
# - --template vue          ← 使用 Vue 模板
```

**执行后会看到：**
```
✔ Select a framework: › Vue
✔ Select a variant: › JavaScript

Scaffolding project in /Users/jim/.../webNaivgator_homepage_edu...

Done. Now run:

  npm install
  npm run dev
```

**生成的文件结构：**
```
webNaivgator_homepage_edu/
├── .gitignore              ← Git 忽略文件配置
├── index.html              ← HTML 入口（自动生成）
├── package.json            ← 依赖配置（自动生成）
├── vite.config.js          ← Vite 配置（自动生成）
├── public/                 ← 静态资源文件夹
│   └── vite.svg
└── src/                    ← 源代码文件夹
    ├── App.vue             ← 主组件
    ├── main.js             ← 入口文件
    ├── style.css           ← 样式文件
    ├── assets/             ← 资源文件夹
    └── components/         ← 组件文件夹
```

---

## 第2步：安装依赖（2分钟）

```bash
# 安装 package.json 中声明的所有依赖
npm install
```

**执行后会看到：**
```
added 45 packages, and audited 46 packages in 5s
```

**生成的内容：**
```
webNaivgator_homepage_edu/
├── node_modules/           ← 所有依赖包（新增）
├── package-lock.json       ← 版本锁定文件（新增）
└── ... 其他文件
```

---

## 第3步：启动开发服务器（1分钟）

```bash
npm run dev
```

**执行后会看到：**
```
  VITE v7.3.1  ready in 298 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**测试：**
1. 打开浏览器访问 http://localhost:5173/
2. 应该看到 Vite + Vue 的默认页面
3. 尝试修改 `src/App.vue` 中的文字，观察热更新

**如果看到页面，说明基础环境搭建成功！✅**

---

## 第4步：理解项目结构（10分钟）

### 📁 关键文件说明

#### 1️⃣ `package.json` - 项目配置

```json
{
  "name": "webnavigator-homepage-edu",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",           // 开发模式
    "build": "vite build",   // 构建生产版本
    "preview": "vite preview" // 预览构建结果
  },
  "dependencies": {
    "vue": "^3.5.25"         // Vue 框架
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.2",  // Vite 的 Vue 插件
    "vite": "^7.3.1"         // Vite 构建工具
  }
}
```

---

#### 2️⃣ `index.html` - HTML 入口

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vite + Vue</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

**关键点：**
- `<div id="app"></div>` - Vue 应用挂载点
- `<script type="module" src="/src/main.js"></script>` - 引入 JS 入口

---

#### 3️⃣ `src/main.js` - JavaScript 入口

```javascript
import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')
```

**执行流程：**
1. 导入 Vue 的 `createApp` 函数
2. 导入全局样式 `style.css`
3. 导入主组件 `App.vue`
4. 创建 Vue 应用并挂载到 `#app`

---

#### 4️⃣ `src/App.vue` - 主组件

```vue
<script setup>
import { ref } from 'vue'

const count = ref(0)
</script>

<template>
  <div>
    <h1>Hello Vue!</h1>
    <button @click="count++">Count: {{ count }}</button>
  </div>
</template>

<style scoped>
h1 {
  color: blue;
}
</style>
```

**Vue 单文件组件（SFC）结构：**
- `<script setup>` - JavaScript 逻辑
- `<template>` - HTML 模板
- `<style scoped>` - CSS 样式（scoped 表示只作用于当前组件）

---

#### 5️⃣ `vite.config.js` - Vite 配置

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
```

**作用：**
- 配置 Vite 构建工具
- 加载 Vue 插件（让 Vite 能处理 .vue 文件）

---

## 第5步：开始编写代码（这是主要工作）

### 5.1 添加 Tailwind CSS（30分钟）

```bash
# 安装 Tailwind CSS
npm install -D tailwindcss @tailwindcss/vite
```

**修改 `vite.config.js`：**
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [vue(), tailwindcss()],
})
```

**修改 `src/style.css`：**
```css
@import "tailwindcss";

/* 你的自定义样式 */
```

---

### 5.2 创建组件（2-3小时）

**组件拆分策略：**

```
src/
├── App.vue                      ← 主应用（导航栏 + 组件组合）
└── components/
    ├── HeroSection.vue          ← 标题区域
    ├── AbstractSection.vue      ← 摘要
    ├── MethodSection.vue        ← 方法
    ├── ResultsSection.vue       ← 结果
    ├── VisualizationSection.vue ← 可视化
    ├── CitationSection.vue      ← 引用
    └── FooterSection.vue        ← 页脚
```

**创建第一个组件：**

```bash
mkdir -p src/components
touch src/components/HeroSection.vue
```

**编辑 `HeroSection.vue`：**
```vue
<script setup>
const title = "WebNavigator"
const subtitle = "Global Web Navigation via Interaction Graph Retrieval"
</script>

<template>
  <section class="py-20 bg-gradient-to-b from-blue-50 to-white">
    <div class="max-w-5xl mx-auto px-4 text-center">
      <h1 class="text-5xl font-bold text-gray-900 mb-6">
        {{ title }}
      </h1>
      <p class="text-xl text-gray-600">
        {{ subtitle }}
      </p>
    </div>
  </section>
</template>
```

**在 `App.vue` 中使用：**
```vue
<script setup>
import HeroSection from './components/HeroSection.vue'
</script>

<template>
  <div class="min-h-screen">
    <HeroSection />
  </div>
</template>
```

---

### 5.3 添加数据可视化（1-2小时）

```bash
# 安装 ECharts
npm install echarts vue-echarts
```

**创建可视化组件：**
```vue
<script setup>
import { ref } from 'vue'
import VChart from 'vue-echarts'

const option = ref({
  title: { text: 'Example Chart' },
  xAxis: { type: 'category', data: ['A', 'B', 'C'] },
  yAxis: { type: 'value' },
  series: [{ data: [10, 20, 30], type: 'bar' }]
})
</script>

<template>
  <v-chart :option="option" style="height: 400px" />
</template>
```

---

## 第6步：配置 GitHub Pages 部署（30分钟）

### 6.1 修改 `vite.config.js`

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [vue(), tailwindcss()],
  base: '/webNaivgator_homepage_edu/',  // ← 重要！改成你的仓库名
})
```

---

### 6.2 创建 GitHub Actions 配置

```bash
mkdir -p .github/workflows
touch .github/workflows/deploy.yml
```

**编辑 `deploy.yml`：**
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

---

### 6.3 推送到 GitHub

```bash
# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: Setup project"

# 添加远程仓库（替换成你的仓库地址）
git remote add origin https://github.com/你的用户名/webNaivgator_homepage_edu.git

# 推送到 GitHub
git push -u origin main
```

---

### 6.4 在 GitHub 启用 Pages

1. 进入仓库 Settings
2. 左侧菜单找到 "Pages"
3. Source 选择 "GitHub Actions"
4. 等待部署完成（约1-2分钟）
5. 访问 `https://你的用户名.github.io/webNaivgator_homepage_edu/`

---

## 📊 完整时间估算

| 步骤 | 时间 | 难度 |
|------|------|------|
| 1. 创建项目骨架 | 5分钟 | ⭐ |
| 2. 安装依赖 | 2分钟 | ⭐ |
| 3. 启动开发服务器 | 1分钟 | ⭐ |
| 4. 理解项目结构 | 10分钟 | ⭐⭐ |
| 5. 添加 Tailwind CSS | 30分钟 | ⭐⭐ |
| 6. 创建组件 | 2-3小时 | ⭐⭐⭐ |
| 7. 添加可视化 | 1-2小时 | ⭐⭐⭐ |
| 8. 配置部署 | 30分钟 | ⭐⭐ |
| **总计** | **5-7小时** | |

---

## 🎯 学习建议

### 第一天（2小时）
- ✅ 完成步骤 1-4
- ✅ 理解项目结构
- ✅ 能够启动开发服务器

### 第二天（3小时）
- ✅ 添加 Tailwind CSS
- ✅ 创建 2-3 个简单组件
- ✅ 理解组件通信

### 第三天（2小时）
- ✅ 完成所有组件
- ✅ 添加样式和交互

### 第四天（1小时）
- ✅ 配置 GitHub Pages
- ✅ 部署上线

---

## ❓ 常见问题

### Q1: 为什么不手动创建 package.json？

**A:** 因为：
1. 格式复杂，容易出错
2. 缺少必要的配置
3. 脚手架会自动生成最佳实践的配置

### Q2: 我可以用其他模板吗？

**A:** 可以！Vite 支持多种模板：
```bash
npm create vite@latest -- --template vue-ts  # Vue + TypeScript
npm create vite@latest -- --template react   # React
npm create vite@latest -- --template vanilla # 纯 JavaScript
```

### Q3: 如果我想从头手写所有配置怎么办？

**A:** 不推荐，但如果你想学习：
1. 先用脚手架生成项目
2. 研究每个文件的作用
3. 理解后再尝试手写

---

## 🚀 下一步

现在你已经理解了完整流程，准备好开始实际操作了吗？

我可以：
1. **立即帮你创建项目** - 运行脚手架命令
2. **逐步讲解每个文件** - 深入理解每个配置
3. **直接开始编写组件** - 边做边学

你想从哪里开始？
